const serverPort = 3001;
const serverAddress = `http://localhost:${serverPort}`; 

const authServerPort = 3002;
const authServerAddress = `http://localhost:${authServerPort}`; 

export {serverPort, serverAddress, authServerAddress, authServerPort};